export enum UpdateType {
  LAYOUT = "layout",
  COMPONENT = "component",
}

export enum ComponentType {
  CHAT = "chat",
}

export enum UpdateDestination {
  CLIENT = "client",
  SERVER = "server",
}
